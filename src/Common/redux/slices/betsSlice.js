import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const betsSlice = createSlice({
    name: "bets",
    initialState,
    reducers: {

    },
});

export const {

} = betsSlice.actions;

export default betsSlice.reducer;
